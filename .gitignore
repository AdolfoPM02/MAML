.venv-maml/
__pycache__/
*.pyc
.ipynb_checkpoints/
outputs/
models/
logs/
# Modelos: ignorar todos los .zip EXCEPTO el entregable best_agent.zip (raíz del ZIP).
# models/best_agent.zip sigue ignorado porque models/ está excluido como directorio.
*.zip
!best_agent.zip
best_duckie_agent.zip
# Vídeos generados (p. ej. outputs/best_agent_loop_empty.mp4): no se versionan.
*.mp4
# Artefactos de compilación LaTeX del report (se versiona report/report.tex y Report.pdf)
report/*.aux
report/*.log
report/*.out
report/report.pdf
*:Zone.Identifier

# Material de referencia local (no se versiona; solo Challenge_RL.ipynb va al repo)
RL_TragaPerras_1_(Preguntas).ipynb
RL_Monte_Carlo_(Preguntas).ipynb
Desafío Final MSc AI_ Conducción Autónoma.pptx
